// run-mission.ts — Legacy entry point kept for TypeScript IDE tooling.
// The active pipeline is now run via: node scripts/run-mission.mjs
// (which has its own self-contained implementation with real RSS fetching)
export {};
